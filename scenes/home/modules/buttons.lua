local buttons = {}

    function buttons.load()
    end
    
    function buttons.update(dt)
    end

    function buttons.mousepressed(x, y, click)
    end

    function buttons.keypressed(key)
    end

    function buttons.draw()
    end

return buttons