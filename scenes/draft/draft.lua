local draft = {}
    draft.sceneName = "DRAFT"

    function draft.load()
    end

    function draft.unload()
    end

    function draft.update(dt)
    end

    function draft.draw()
    end

    function draft.keypressed(key)
    end

    function draft.mousepressed(x, y, click)
    end

return draft